 <form method="post" class="mb-4">
        
        {{ form.as_p }}
        <button type="submit" class="btn btn-primary">Predict</button>
    </form>

    {% if result %}
        <div class="alert alert-info">
            <h4>📌 Results</h4>
            <p><strong>Decision Tree:</strong> {{ result.dt_pred }} (Confidence: {{ result.dt_conf }})</p>
            <p><strong>Sentiment:</strong> {{ result.svm_pred }} (Confidence: {{ result.svm_conf }})</p>
            <p><strong>Hybrid Prediction:</strong> {{ result.hybrid_pred }} (Confidence: {{ result.hybrid_conf }})</p>
            {% if result.risk_factors %}
                <p><strong>⚠️ Risk Factors:</strong></p>
                <ul><!DOCTYPE html>
<html>
<head>
  <title>Student Prediction</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<div class="container py-5">
  <div class="card p-4 shadow">
    <h2 class="mb-4">Predict Degree Completion</h2>

    <form method="POST">
      {% csrf_token %}
      {% for field in form %}
        <div class="mb-3">
          <label class="form-label">{{ field.label }}</label>
          {{ field }}
          {% if field.errors %}
            <div class="text-danger">{{ field.errors }}</div>
          {% endif %}
        </div>
      {% endfor %}
      <button class="btn btn-primary">Predict</button>
    </form>

    {% if result %}
      <hr>
      <h4>Results:</h4>
      <p><strong>Academic Prediction:</strong> {{ result.0 }} (Confidence: {{ result.1|floatformat:2 }})</p>
      <p><strong>Sentiment:</strong> {{ sentiment.0 }} (Confidence: {{ sentiment.1|floatformat:2 }})</p>
      <p><strong>Hybrid Prediction:</strong> {{ hybrid.0 }} (Confidence: {{ hybrid.1|floatformat:2 }})</p>

      <h5 class="mt-3">Dropout Risk Indicators:</h5>
      <ul>
        {% for r in risks %}
          <li>{{ r }}</li>
        {% empty %}
          <li>No major risks detected.</li>
        {% endfor %}
      </ul>
    {% endif %}
  </div>
</div>
</body>
</html>

                    {% for risk in result.risk_factors %}
                        <li>{{ risk }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <p>✅ No major risk factors identified.</p>
            {% endif %}
        </div>
    {% endif %}